# Wiegand Nano Bridge

- Reads two Wiegand readers (26/34) and sends UID to ESP32 over UART.
- Output format: `1,UID` or `2,UID` with `\n` line ending.
- UART speed: 115200 baud.

## Files
- `wiegand_nano.ino`
